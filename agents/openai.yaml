interface:
  display_name: "Ketupa Antenna Designer"
  short_description: "Offline multi-family HFSS antenna design"
  default_prompt: "Use $ketupa-antenna-designer to interpret my request offline, reject ambiguity instead of falling back to another family, and then generate the exact HFSS starting model."
